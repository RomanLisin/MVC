@model IEnumerable<Academy_HW.Models.Direction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DirectionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DirectionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DirectionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DirectionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DirectionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
